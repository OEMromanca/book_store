pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/main']], userRemoteConfigs: [[url: 'https://github.com/OEMromanca/book_store.git']]])
            }
        }
        
        stage('Build Docker Image') {
            steps {
                script {
                    sh 'docker build -t book_store:${env.BUILD_NUMBER} .'
                }
            }
        }
        
        stage('Run Docker Containers') {
            steps {
                sh 'docker run  --rm  --name book_store  -p 8080:8080 book_store:${env.BUILD_NUMBER}'
            }
        }
    }
}
